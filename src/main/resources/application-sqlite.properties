# SQLite support removed from this repository.
# File retained for historical reasons; its contents were removed
# during the cleanup to avoid runtime and compatibility issues with
# Hibernate. If you need SQLite again, re-add a driver and a
# Hibernate-6-compatible dialect and re-create this profile.
